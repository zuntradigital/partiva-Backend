PORT=5000

JWT_SECRET=YOUR_LONG_RANDOM_SECRET

DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

JWT_EXPIRES_IN=1d

BCRYPT_SALT_ROUNDS=10
INVITATION_TOKEN_EXPIRY_HOURS=48

ADMIN_APP_URL=http://localhost:3001

ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3002

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=notifications@partiva.tech
SMTP_PASSWORD=YOUR_EMAIL_PASSWORD
SMTP_FROM=notifications@partiva.tech

# reCAPTCHA v2 server-side verification secret. The value below is Google's
# official public test key (always passes, zero real protection) -- fine for
# local development, but MUST be replaced with a real secret key from
# https://www.google.com/recaptcha/admin before this runs in production.
RECAPTCHA_SECRET_KEY=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe